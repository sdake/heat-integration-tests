# The machine to provision (required)
MACHINE=

# SSH Key file location
#SSH_KEY_FILE=~/.ssh/heat-test-id_rsa

# SSH Username (defaults to root)
#SSH_USER=root

# Fedora version to provision the test box with
HOST_VERSION=17
