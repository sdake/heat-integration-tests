# The machine to provision (required)
MACHINE=

# SSH Key file location
#SSH_KEY_FILE=~/.ssh/heat-test-id_rsa

# SSH Username (defaults to root)
#SSH_USER=root

# Fedora version to provision the test box with
HOST_VERSION=17

# Copy the Fedora ISO from the local box if it is already present, rather than
# downloading directly from Fedora to the test machine.
#ALLOW_ISO_COPY=yes
